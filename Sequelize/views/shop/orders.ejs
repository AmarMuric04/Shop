    <%- include('../includes/head.ejs') %>
</head>

<body>
    <%- include('../includes/navigation.ejs') %>
    <main>
        <% if (orders.length > 0) { %> 
            <ul class="orders-content-list">
                <% orders.forEach((order) => { %>
                    <li>
                        <h1># <%= order.id %></h1>
                        <ul>
                            <% order.products.forEach((product) => { %>
                                <li>
                                    <h1><%= product.title %></h1>
                                    <p><%= product.price %></p>
                                    <p>quantity: <%= product.orderItem.quantity ? product.orderItem.quantity : 0%></p>
                                </li>
                            <% }); %>
                        </ul>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <h1>Nothing there!</h1>
        <% } %>
    </main>
    <%- include('../includes/end.ejs') %>
